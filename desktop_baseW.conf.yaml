- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.config/sway: sway/.config/sway
    ~/.config/swaylock: swaylock/.config/swaylock
    ~/.config/waybar: waybar/.config/waybar
    ~/.config/wofi: wofi/.config/wofi
