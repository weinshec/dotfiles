- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.config/taskwarrior/taskrc_local: taskwarrior/taskrc_local_private

    ~/.config/vdirsyncer: vdirsyncer/.config/vdirsyncer
    ~/.config/khal: khal/.config/khal
    ~/.config/khard: khard/.config/khard
    ~/.config/todoman: todoman/.config/todoman
